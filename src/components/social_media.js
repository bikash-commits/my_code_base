import React from 'react';
import {map} from 'ramda'
 
export default class SocialMedia extends React.Component {
  constructor(props){
    super(props);
  }

  render() {
    const SocialMediaDetails = this.props.handles;
    }
    return(
    );
  }
}